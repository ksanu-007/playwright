appId: com.infinite.netsfere
tags:
  - cross-platform
  - android-send-location
---
# =============================================================================
# Android sends current location in a conversation
# Requires: CONVERSATION_NAME
# =============================================================================
- launchApp:
    clearState: false
- runFlow:
    when:
      visible:
        id: "com.infinite.netsfere:id/send_message_edittext"
    commands:
      - back
- extendedWaitUntil:
    visible:
      id: "com.infinite.netsfere:id/new_conversation_fab"
    timeout: 15000
- scrollUntilVisible:
    element:
      text: ${CONVERSATION_NAME}
    direction: DOWN
    timeout: 30000
    speed: 40
- tapOn:
    text: ${CONVERSATION_NAME}
- assertVisible:
    id: "com.infinite.netsfere:id/send_message_edittext"
# Handle any stale permission dialogs
- runFlow:
    when:
      visible: "While using the app"
    commands:
      - tapOn: "While using the app"
- runFlow:
    when:
      visible: "Allow"
    commands:
      - tapOn: "Allow"
- tapOn:
    id: "com.infinite.netsfere:id/insert_attachment_button"
- extendedWaitUntil:
    visible:
      id: "com.infinite.netsfere:id/attach_location_textView"
    timeout: 15000
- tapOn:
    id: "com.infinite.netsfere:id/attach_location_textView"
- extendedWaitUntil:
    visible:
      id: "com.infinite.netsfere:id/action_done"
    timeout: 15000
- tapOn:
    id: "com.infinite.netsfere:id/action_done"
- assertVisible:
    id: "com.infinite.netsfere:id/send_message_edittext"
- assertVisible:
    text: ".*My Location.*"
    id: "com.infinite.netsfere:id/file_name_textView_bottom"
